<h1>Posts</h1>
<% posts.each do |post| %>
  <h2><%= post.title %></h2>
  <% memos = post.ordered_memos %>
  <% memos.each do |memo| %>
    <div>
      <%= memo.body %>
    </div>
  <% end %>
  <%= link_to 'Edit', routes.edit_post_path(post.id) %>
<% end %>
